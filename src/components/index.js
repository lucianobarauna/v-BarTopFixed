import Vue from 'vue'
import BarFixed from './BarFixed.vue'

Vue.component(BarFixed)

export default BarFixed

// const Components = {
//   BarFixed
// }

// Object.keys(Components).forEach(name => {
//   Vue.component(name, Components[name])
// })

// export default Components
